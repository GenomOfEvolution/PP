#### **1. Синхронная версия (последовательный `for`)**
```cpp
double pi_approx = 0.0;
for (int i = 0; i < terms; ++i) { ... }
return pi_approx * 4.0;
```
- **Особенности**:
  - Работает в одном потоке.
  - Максимальная точность (порядок сложений фиксирован).
  - Минимальная производительность для больших `terms`.
- **Корректность**: ✅ Идеальная.
- **Скорость**: ⚠️ Медленно (линейная зависимость от `terms`).

---

#### **2. Параллельный `for` (некорректная версия)**
```cpp
#pragma omp parallel for
for (int i = 0; i < terms; ++i) {
    pi_approx += term; // Гонка данных!
}
```
- **Особенности**:
  - Все потоки одновременно записывают в общую переменную `pi_approx`.
  - **Гонка данных** → часть вычислений теряется.
- **Корректность**: ❌ Результат сильно искажён (например, 3.11 вместо 3.14).
- **Скорость**: ✅ Быстро (но бесполезно из-за ошибок).

---

#### **3. Параллельный `for` + `atomic`**
```cpp
#pragma omp parallel for
for (int i = 0; i < terms; ++i) {
    #pragma omp atomic
    pi_approx += term;
}
```
- **Особенности**:
  - `atomic` гарантирует атомарность операции `+=`.
  - **Проблемы**:
    - Атомарные операции для `double` не поддерживаются на всех платформах.
    - Высокие накладные расходы из-за частых блокировок.
- **Корректность**: ✅ Точность близка к синхронной версии (но возможны погрешности из-за порядка сложений).
- **Скорость**: ⚠️ Почти как последовательная версия (блокировки убивают параллелизм).

---

#### **4. Параллельный `for` + `reduction` (оптимальная версия)**
```cpp
#pragma omp parallel for reduction(+:pi_approx)
for (int i = 0; i < terms; ++i) {
    pi_approx += term;
}
```
- **Особенности**:
  - OpenMP автоматически создаёт **локальные копии** `pi_approx` для каждого потока.
  - После цикла все локальные значения суммируются в глобальную переменную.
  - Минимум синхронизации → максимальная производительность.
- **Корректность**: ✅ Точность совпадает с синхронной версией.
- **Скорость**: ✅ Максимальная (линейное ускорение на многопроцессорных системах).

---

### ⚙️ Как работают директивы OpenMP

#### **1. `#pragma omp parallel for`**
- **Что делает**: Распределяет итерации цикла между потоками.
- **Особенности**:
  - Каждый поток получает свою часть итераций.
  - **Опасность**: Если в цикле используется общая переменная (например, `pi_approx`), возникает **гонка данных**.
- **Пример ошибки**:
  ```cpp
  #pragma omp parallel for
  for (i = 0; i < N; i++) sum += a[i]; // Гонка данных на `sum`
  ```

---

#### **2. `#pragma omp atomic`**
- **Что делает**: Гарантирует атомарность следующей операции присваивания (например, `x += y`).
- **Ограничения**:
  - Работает только для простых операций (`+=`, `-=`, `=`, etc.).
  - **Не поддерживается для `double` на некоторых платформах** (например, ARM без специальных флагов).
  - Медленно из-за блокировок шины памяти.
- **Когда использовать**: Для редких обновлений глобальных переменных.

---

#### **3. `reduction(op:var)`**
- **Что делает**:
  1. Создаёт **приватную копию** `var` для каждого потока.
  2. В конце объединяет все копии через операцию `op` (например, `+`).
- **Поддерживаемые операции**:
  - `+`, `-`, `*`, `&`, `|`, `^`, `&&`, `||`, `max`, `min`.
- **Преимущества**:
  - Нет гонок данных.
  - Нет явной синхронизации → высокая производительность.
- **Пример**:
  ```cpp
  #pragma omp parallel for reduction(+:sum)
  for (i = 0; i < N; i++) sum += a[i];
  ```


# Разница между `private`, `firstprivate` и `lastprivate` в OpenMP

## 1. `private(x)`
### Как работает:
- Создает **независимую копию** переменной `x` для каждого потока
- **Не инициализируется** исходным значением (остается мусором в памяти)
- Изменения в приватных копиях **не влияют** на исходную переменную

### Вывод программы:
```text
Thread 2 | i=8 → x=8
Thread 2 | i=9 → x=9
Thread 2 | i=10 → x=10
Thread 1 | i=4 → x=4
Thread 1 | i=5 → x=5
Thread 1 | i=6 → x=6
Thread 1 | i=7 → x=7
Thread 0 | i=0 → x=0
Thread 0 | i=1 → x=1
Thread 0 | i=2 → x=2
Thread 0 | i=3 → x=3
After parallel region: x = 44  // Исходное значение сохранено
```

### Особенности:
- Значение `x` внутри потоков **непредсказуемо**, если не присвоить явно
- После параллельного региона исходная переменная остается без изменений

---

## 2. `firstprivate(x)`
### Как работает:
- Создает приватную копию для каждого потока, **инициализированную значением из внешнего контекста**
- Все потоки начинают с `x = 44`
- Изменения в приватных копиях **не влияют** на исходную переменную

### Модифицированный код:
```cpp
#pragma omp parallel for firstprivate(x)
for (int i = 0; i <= 10; i++) {
    printf("Thread %d | i=%d → initial x=%d\n", 
           omp_get_thread_num(), i, x);
    x = i;  // Перезапись после вывода
}
```

### Вывод (фрагмент):
```text
Thread 0 | i=0 → initial x=44
Thread 0 | i=1 → initial x=44
...
Thread 1 | i=4 → initial x=44
Thread 1 | i=5 → initial x=44
...
After parallel region: x = 44  // Исходное значение сохранено
```

### Особенности:
- Идеально для **настройки начального состояния** в каждом потоке
- Особенно полезно для сложных типов (например, объектов C++)

---

## 3. `lastprivate(x)`
### Как работает:
- Создает приватные копии как `private`
- **Копирует значение** из **последней итерации** (в порядке выполнения) обратно в исходную переменную
- Порядок определяется:
  - Для `static` распределения: последняя итерация в последовательном порядке
  - Для `dynamic`: последняя выполненная итерация

### Модифицированный код:
```cpp
#pragma omp parallel for lastprivate(x)
for (int i = 0; i <= 10; i++) {
    x = i;
    printf("Thread %d | i=%d → x=%d\n", 
           omp_get_thread_num(), i, x);
}
printf("After parallel region: x = %d\n", x);  // Ключевое изменение!
```

### Вывод:
```text
...
Thread 2 | i=10 → x=10   ← Последняя итерация в расписании
After parallel region: x = 10  // Значение скопировано из последней итерации!
```

### Особенности:
- **Небезопасно для неупорядоченных циклов** (результат зависит от распределения итераций)
- Может вызвать гонку данных, если несколько потоков претендуют на "последнюю" итерацию
- Часто используется с `schedule(static)` для предсказуемости

---

## 🆚 Сравнительная таблица

| Директива       | Инициализация       | Влияние на исходную переменную | Особые случаи использования       |
|-----------------|---------------------|-------------------------------|-----------------------------------|
| `private(x)`    | Не инициализируется | Не влияет                     | Временные переменные в расчетах   |
| `firstprivate(x)`| Копирует внешнее значение | Не влияет              | Инициализация счетчиков/состояний |
| `lastprivate(x)`| Не инициализируется | **Перезаписывает** значением из последней итерации | Возврат финального состояния      |